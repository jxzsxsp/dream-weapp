<view class="back-content flex-column">
  <image src="/assets/image/banner@3x.png"></image>
</view>

<view class="main-content flex-column">
  <view wx:if="{{order}}" class="order-content flex-column">
    <text class="memo">最近一张订单的位置</text>
  </view>

  <view class="item-content flex-column">
    <block wx:for="{{items}}" wx:key="index">
      <view class="item-container flex">
        <view class="content">
          <view class="head flex">
            <view class="title">{{item.title}}</view>
            <view class="price">{{item.price}}</view>
          </view>
          <view class="desc">{{item.desc}}</view>
          <view class="button flex">去验布</view>
        </view>

        <view class="image">
          <image src="{{item.image}}"></image>
        </view>
      </view>
    </block>
    <text class="memo">“基础检验”服务包含：米数、重量、克重、门幅的测量</text>
  </view>
</view>