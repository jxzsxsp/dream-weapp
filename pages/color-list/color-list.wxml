<view class="container">
  <view class="search-bar-container">
    <view class="search-bar">
      <view class="search-icon"/>
      <input 
        class="search-input" 
        placeholder="请输入要搜索的色号" 
        placeholder-class="search-placeholder" 
        confirm-type="search"
        bindinput="onInput"
        bind:confirm="beginSearch"
      />
    </view>
  </view>  
  <view class="color-total-container">
    <view wx:if="{{colorList.length === 0}}" class="no-color-container">
      <text class="no-color-title">无匹配的色号</text>
      <text class="no-color-description">请确认是否输入正确</text>
      <text class="no-color-description">或者换个色号试试</text>
    </view>
    <button class="color-container" wx:for="{{colorList}}" wx:key="{{index}}" bind:tap="colorClicked" data-index="{{index}}">
      <view style="background: {{item.hexColor}}" class="color-color"/>
      <text class="color-title">{{item.name}}</text>
    </button>
  </view>

</view>