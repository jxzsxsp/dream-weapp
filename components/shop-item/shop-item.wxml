<view class='item-container'>
  <view class='head-container'>
    <view class='shop-img'>
      <image src="{{shop.logo}}"></image>
    </view>

    <view class='shop-content' bind:tap="onDetail">
      <text class='shop-name'>{{shop.name}}</text>
      <view class='main-business'>
        <text class='business-title'>主营</text>
        <block wx:for="{{shop.shopBusinessDtos}}" wx:key="index">
          <block wx:if="{{index < 3}}">
            <text class='business-name'>{{item.businessName}}</text>
          </block>
          <block wx:if="{{index === 3}}">
            <text class='business-name'>...</text>
          </block>
        </block>
      </view>
    </view>

    <view class='operate-btn'>
      <view wx:if="{{shop.isFollow === 0}}" class='btn' bind:tap="onFollow">关注</view>
      <view wx:if="{{shop.isFollow === 1}}" class='btn-active' bind:tap="onCancel">已关注</view>
    </view>
  </view>

  <view wx:if="{{shop.showDetail}}" class='detail-container'>
    <view class='line'></view>
    <view class='detail-item'>
      <text class='detail-title'>电话</text>
      <text class='detail-desc'>{{shop.mobile}}</text>
    </view>
    <view class='detail-item'>
      <text class='detail-title'>地址</text>
      <text class='detail-desc'>{{shop.provinceName}}{{shop.cityName}}{{shop.areaName}}{{shop.address}}</text>
    </view>
  </view>
</view>